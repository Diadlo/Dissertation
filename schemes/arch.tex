\begin{tikzpicture}[
        ->,>=stealth',node
        distance=1.5cm, semithick
    ]

    \tikzstyle{block} = [rectangle,draw,minimum height=0.8cm]

    \node[block] (app)   {Целевое приложение};
    \node[block] (preload) [above of=app] {Подгружаемый модуль};
    \node[block] (lib)    [above of=preload] {libQtWidgets};
    \node[block] (x11)    [above of=lib]     {X11};
    \node[block] (user)   [above of=x11] {Пользователь};

	\node[block] (ctrl)   [left=2cm of user] {Приложение управления};
    \node[block] (server) [below of=ctrl] {Сервер управления};



    \path[]
        (app)     edge (preload)
        (preload) edge (server)
        (preload) edge (lib)
        (lib)     edge (x11)
        (server)  edge (ctrl)
        (user)    edge (ctrl)
		(ctrl)    edge (user)
        (ctrl)    edge (server)
        (server)  edge (preload)

        (server)  edge (x11)

        (x11)     edge (lib)
        (lib)     edge (preload)
        (preload) edge (app)
        (preload) edge (lib)
        
        (user) edge (x11)
        (x11) edge (user)
    ;
    
    \path[every edge/.append style={<->}]
    ;

\end{tikzpicture}
